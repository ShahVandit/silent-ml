# Install a CUDA build of torch separately to match your driver, e.g.
#   python -m pip install torch --index-url https://download.pytorch.org/whl/cu121
# Versions are lower bounds rather than pins: exact pins break on newer Pythons
# (PyYAML 5.x, for instance, fails to build against Cython 3).
torch>=2.0
numpy>=1.24
PyYAML>=6.0
pytest>=7.0
