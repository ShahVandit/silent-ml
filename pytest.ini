[pytest]
# Collect only this directory's tests. Without an explicit testpaths, a stray
# nested copy of the repo (or another checkout beside it) is also collected and
# pytest fails with "import file mismatch" on the duplicated basenames.
testpaths = tests
